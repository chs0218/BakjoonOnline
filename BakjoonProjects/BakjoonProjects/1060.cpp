#include <iostream>
#include <vector>
using namespace std;
int main()
{
    cin.tie(0);
    cout.tie(0);
    ios::sync_with_stdio(false);

    int L;
    cin >> L;

    vector<int> S(L);
    for (int i = 0; i < L; ++i)
        cin >> S[i];


}